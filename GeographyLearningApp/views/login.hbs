<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>GeoQuizzer Login</title>
        <link rel="stylesheet" type="text/css" href = "css/style.css">
    </head>
    <body>
        {{> topnav}}
        <div class="content">
            {{#if logged_in}}
                <p>You are already logged in as {{user_name}}<br><br>
            {{else}}
            <div class="content">
            <form action="login_worker" method="post">
                Username:<br>
                <input type="text" name="user"><br>
                Password:<br>
                <input type="password" name="pass"><br><br>
                <button class="pageButton" type="submit">Log In</button>
            </form>
            New here? Click below to create an account!
            <a href="account_manager"><button class="pageButton" type="button">Create Account</button></a>
            <p class="wrong">{{wrong}}</p>
            {{/if}}
        </div>
        </div>
    </body>
</html>